/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['lh3.googleusercontent.com','localhost'], // Add external domains you need here
    },
  };
  
  export default nextConfig;
  